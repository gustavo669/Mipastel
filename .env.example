DB_SERVER=(localdb)\MSSQLLocalDB
DB_USER=
DB_PASSWORD=
DB_NAME_NORMALES=MiPastel
DB_NAME_CLIENTES=MiPastel_Clientes
DB_DRIVER=ODBC Driver 17 for SQL Server

SECRET_KEY=your_secret_key_here_change_in_production
ADMIN_PASSWORD_HASH=

HOST=0.0.0.0
PORT=5000
DEBUG=False
LOG_LEVEL=INFO

REDIS_URL=redis://localhost:6379

ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

SESSION_DURATION_HOURS=8
MAX_LOGIN_ATTEMPTS=5
LOGIN_TIMEOUT_SECONDS=300

# Password Hashes
# Generate password hashes using: python utils/generate_password_hashes.py
# IMPORTANT: Never commit actual password hashes to version control
# Store these in your local .env file only

# Example format:
# JUTIAPA1_PASSWORD_HASH=$2b$12$...
# JUTIAPA2_PASSWORD_HASH=$2b$12$...
# ADMIN_PASSWORD_HASH=$2b$12$...

# Note: The current hashes in api/auth.py are PLACEHOLDERS
# You MUST regenerate them with actual passwords before production use
